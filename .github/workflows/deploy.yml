name: 🚀 Auto Deploy to AWS with AI Code Review (Groq)

on:
  push:
    branches:
      - prod_v2
  pull_request_target:
    branches:
      - prod_v2
  workflow_dispatch:

jobs:
  ai_code_review:
    name: 🤖 AI Code Review via Groq
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Validate secrets
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          : "${GROQ_API_KEY:?GROQ_API_KEY is not set}"
          : "${GITHUB_TOKEN:?GITHUB_TOKEN is not set}"
          echo "✅ Secrets are set."

      - name: Generate Git Diff
        run: |
          git fetch --unshallow || true
          git fetch origin prod_v2
          git diff ${{ github.event.before }} ${{ github.sha }} | head -n 2000 | head -c 50000 > diff.txt
          if [ ! -s diff.txt ]; then
            echo "⚠️ No code changes detected — skipping AI review."
            echo "AI Code Review: ✅ Passed — no changes detected." > ai_review_output.txt
            exit 0
          fi

      - name: Run AI Code Review (Groq)
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: |
          set -euo pipefail
          DIFF=$(<diff.txt)
          PAYLOAD=$(jq -n --arg diff "$DIFF" '{
            model: "llama-3.1-8b-instant",
            messages: [
              {role: "system", content: "You are a senior software engineer performing a concise code review. Flag bugs, vulnerabilities, or critical issues."},
              {role: "user", content: "Review the following git diff for potential problems or security issues:\n\n\($diff)"}
            ]
          }')

          RAW_RESPONSE=$(curl -sS --fail https://api.groq.com/openai/v1/chat/completions \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer $GROQ_API_KEY" \
            --data-raw "$PAYLOAD") || exit 1

          REVIEW=$(echo "$RAW_RESPONSE" | jq -r '.choices[0].message.content // empty')
          if [ -z "$REVIEW" ]; then
            REVIEW="⚠️ No review returned from Groq."
          fi

          echo "$REVIEW" | tee ai_review_output.txt

          # Mark workflow as failed if critical issue detected
          if grep -iq "critical issue" ai_review_output.txt; then
            echo "❌ Critical issue detected. Review failed."
            echo "AI Code Review: ❌ Failed — critical issues detected. See details below:" > ai_review_status.txt
            cat ai_review_output.txt >> ai_review_status.txt
            exit 1
          else
            echo "AI Code Review: ✅ Passed. No critical issues found." > ai_review_status.txt
          fi

      - name: Post AI Review as Comment
        if: always()  # Run even if previous step failed
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          COMMENT_BODY=$(jq -Rn --arg text "$(cat ai_review_status.txt)" '$text')

          if [ "${{ github.event_name }}" = "pull_request_target" ]; then
            PR_NUMBER=${{ github.event.pull_request.number }}
            API_URL="${{ github.api_url }}/repos/${{ github.repository }}/issues/$PR_NUMBER/comments"
          else
            API_URL="${{ github.api_url }}/repos/${{ github.repository }}/commits/${{ github.sha }}/comments"
          fi

          HTTP_RESPONSE=$(curl -sS -w "%{http_code}" -o response.json -X POST \
            -H "Authorization: token $GITHUB_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"body\":$COMMENT_BODY}" \
            $API_URL)

          if [ "$HTTP_RESPONSE" -ne 201 ]; then
            echo "⚠️ Failed to post comment. HTTP code: $HTTP_RESPONSE"
            cat response.json
          fi

  deploy:
    name: 🚀 Deploy to AWS (Manual Trigger)
    runs-on: ubuntu-latest
    needs: ai_code_review
    if: github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Add SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.DEPLOY_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H 3.232.21.89 >> ~/.ssh/known_hosts

      - name: Deploy and restart app on AWS
        run: |
          ssh -i ~/.ssh/id_rsa ubuntu@3.232.21.89 "
            cd /home/ubuntu &&
            git fetch origin prod_v2 &&
            git reset --hard origin/prod_v2 &&
            npm install &&
            pm2 restart my-app &&
            echo 'Deployment completed successfully'
          "
