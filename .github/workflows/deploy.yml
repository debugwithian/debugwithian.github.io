name: 🚀 Auto Deploy to AWS with AI Code Review (Groq)

on:
  pull_request:
    branches:
      - prod_v2
    types:
      - opened
      - synchronize
      - reopened
  push:
    branches:
      - prod_v2
  workflow_dispatch:

jobs:
  ai_code_review:
    name: 🤖 AI Code Review
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' && github.event.pull_request.merged == false
    outputs:
      review_status: ${{ steps.set_review_status.outputs.review_status }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y jq curl

      - name: Validate secrets
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          : "${GROQ_API_KEY:?GROQ_API_KEY is not set}"
          : "${PAT_TOKEN:?PAT_TOKEN is not set}"
          echo "✅ Secrets validated."

      - name: Generate Git Diff for PR branch
        id: generate_diff
        run: |
          set -euo pipefail
          git fetch origin prod_v2
          git fetch origin +refs/pull/${{ github.event.pull_request.number }}/head:pr_branch
          git checkout pr_branch
          BASE=$(git merge-base HEAD origin/prod_v2)

          if ! git diff $BASE HEAD --quiet; then
            git diff $BASE HEAD > diff.txt
            echo "diff_exists=true" >> $GITHUB_OUTPUT
          else
            echo '{"status":"PASSED","body":"✅ No issues detected."}' > ai_review_output.txt
            echo "diff_exists=false" >> $GITHUB_OUTPUT

      - name: Run AI Code Review (Groq)
        if: steps.generate_diff.outputs.diff_exists == 'true'
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: |
          set -euo pipefail
          DIFF=$(<diff.txt)

          PAYLOAD=$(jq -n --arg diff "$DIFF" '{
            model: "llama-3.1-8b-instant",
            messages: [
              {
                role: "system",
                content: "You are a senior software engineer performing a strict code review. Return ONLY a single valid JSON object: {\"status\": \"CRITICAL_ISSUE|IMPROVEMENTS|PASSED\", \"body\": \"Markdown PR comment\"}. Format all code snippets with triple backticks and language hints (```html, ```js), use actual newlines, escape quotes properly, and include headings, bullet points, and preformatted code blocks. Do not return text outside JSON."
              },
              {
                role: "user",
                content: ("Review the following git diff for potential problems (logic, syntax, style, performance, security). Return only one of: CRITICAL_ISSUE if critical issues exist, IMPROVEMENTS if suggestions, or PASSED if no issues. Format your response in GitHub Markdown with headings, bullets, and preformatted code blocks.\n\n" + $diff)
              }
            ]
          }')

          RAW_RESPONSE=$(curl -sS --fail https://api.groq.com/openai/v1/chat/completions \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer $GROQ_API_KEY" \
            --data-raw "$PAYLOAD") || exit 1

          REVIEW_RAW=$(echo "$RAW_RESPONSE" | jq -r '.choices[0].message.content // empty')

          if [ -z "$REVIEW_RAW" ]; then
            REVIEW_RAW='{"status":"PASSED","body":"✅ No issues detected."}'
          fi

          echo "$REVIEW_RAW" | tee ai_review_output.txt

      - name: Analyze AI Review
        id: set_review_status
        run: |
          set -euo pipefail
          REVIEW_FILE="ai_review_output.txt"
          RAW=$(<"$REVIEW_FILE")

          # Extract JSON object from AI response
          INNER_JSON=$(echo "$RAW" | grep -oP '\{.*\}' | jq -c '.' 2>/dev/null || true)

          if [ -n "$INNER_JSON" ]; then
              DETECTED_STATUS=$(jq -r '.status' <<< "$INNER_JSON")
              EXTRACTED_BODY=$(jq -r '.body' <<< "$INNER_JSON")

              # Remove nested JSON/code fences if present
              if [[ "$EXTRACTED_BODY" =~ ^\`\`\` ]]; then
                  EXTRACTED_BODY=$(echo "$EXTRACTED_BODY" | sed -E '1d;$d')
                  EXTRACTED_BODY=$(echo "$EXTRACTED_BODY" | jq -r '.body // .' 2>/dev/null || echo "$EXTRACTED_BODY")
              fi
          else
              DETECTED_STATUS="PASSED"
              EXTRACTED_BODY="$RAW"
          fi

          # Normalize status to a single one
          case "$DETECTED_STATUS" in
            CRITICAL* ) NORMALIZED="CRITICAL" ;;
            IMPROVEMENTS ) NORMALIZED="IMPROVEMENTS" ;;
            PASSED ) NORMALIZED="PASSED" ;;
            * ) NORMALIZED="IMPROVEMENTS" ;;
          esac

          # Prepend status for PR comment
          EXTRACTED_BODY="Status: $NORMALIZED\n\n$EXTRACTED_BODY"

          # Escape safely for JSON
          SAFE_BODY=$(printf "%s" "$EXTRACTED_BODY" | jq -Rs '.')

          # Write cleaned JSON back
          printf '{"status":"%s","body":%s}\n' "$NORMALIZED" "$SAFE_BODY" > "$REVIEW_FILE"

          # Write PR comment
          echo -e "$EXTRACTED_BODY" > ai_review_status.txt
          echo "review_status=$NORMALIZED" >> $GITHUB_OUTPUT

      - name: Post AI Review as PR Comment
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          set -euo pipefail
          COMMENT=$(<ai_review_status.txt)
          JSON_BODY=$(jq -Rs --arg text "$COMMENT" '{body: $text}')
          PR_NUMBER=${{ github.event.pull_request.number }}
          REPO=${{ github.repository }}
          API_URL="https://api.github.com/repos/$REPO/issues/$PR_NUMBER/comments"
          curl -sS -X POST \
               -H "Authorization: token $PAT_TOKEN" \
               -H "Content-Type: application/json" \
               -d "$JSON_BODY" \
               "$API_URL"

      - name: Fail PR if Critical
        if: steps.set_review_status.outputs.review_status == 'CRITICAL'
        run: |
          echo "❌ CRITICAL issues detected. Merge blocked until resolved."
          exit 1

  deploy:
    name: 🚀 Deploy to AWS
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/prod_v2'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Add SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.DEPLOY_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H 3.232.21.89 >> ~/.ssh/known_hosts

      - name: Deploy and restart app on AWS
        run: |
          set -euo pipefail
          ssh -i ~/.ssh/id_rsa ubuntu@3.232.21.89 "
            cd /home/ubuntu &&
            git fetch origin prod_v2 &&
            git reset --hard origin/prod_v2 &&
            npm install &&
            pm2 restart all &&
            echo 'Deployment completed successfully'
          "
